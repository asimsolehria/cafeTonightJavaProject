
package MainPackage;

/**
 *
 * @author Abdul Sami
 */
public class CancelDineInItem extends POS
{
 public static void CancelDineItem()
 {
     if(Table1.isSelected())
        {
               a1.remove(PurchasedItems.getSelectedRow()*4+3);
               a1.remove(PurchasedItems.getSelectedRow()*4+2);
               a1.remove(PurchasedItems.getSelectedRow()*4+1);
               a1.remove(PurchasedItems.getSelectedRow()*4);  
               //********************************************
        }else if(Table2.isSelected())
        {
               a2.remove(PurchasedItems.getSelectedRow()*4+3);
               a2.remove(PurchasedItems.getSelectedRow()*4+2);
               a2.remove(PurchasedItems.getSelectedRow()*4+1);
               a2.remove(PurchasedItems.getSelectedRow()*4);   
               //********************************************
        }else if(Table3.isSelected())
        {
               a3.remove(PurchasedItems.getSelectedRow()*4+3);
               a3.remove(PurchasedItems.getSelectedRow()*4+2);
               a3.remove(PurchasedItems.getSelectedRow()*4+1);
               a3.remove(PurchasedItems.getSelectedRow()*4);   
               //********************************************
        }else if(Table4.isSelected())
        {
               a4.remove(PurchasedItems.getSelectedRow()*4+3);
               a4.remove(PurchasedItems.getSelectedRow()*4+2);
               a4.remove(PurchasedItems.getSelectedRow()*4+1);
               a4.remove(PurchasedItems.getSelectedRow()*4);   
               //********************************************
        }else if(Table5.isSelected())
        {
               a5.remove(PurchasedItems.getSelectedRow()*4+3);
               a5.remove(PurchasedItems.getSelectedRow()*4+2);
               a5.remove(PurchasedItems.getSelectedRow()*4+1);
               a5.remove(PurchasedItems.getSelectedRow()*4);   
               //********************************************
        }else if(Table6.isSelected())
        {
               a6.remove(PurchasedItems.getSelectedRow()*4+3);
               a6.remove(PurchasedItems.getSelectedRow()*4+2);
               a6.remove(PurchasedItems.getSelectedRow()*4+1);
               a6.remove(PurchasedItems.getSelectedRow()*4);   
               //********************************************
        }else if(Table7.isSelected())
        {
               a7.remove(PurchasedItems.getSelectedRow()*4+3);
               a7.remove(PurchasedItems.getSelectedRow()*4+2);
               a7.remove(PurchasedItems.getSelectedRow()*4+1);
               a7.remove(PurchasedItems.getSelectedRow()*4);   
               //********************************************
        }else if(Table8.isSelected())
        {
               a8.remove(PurchasedItems.getSelectedRow()*4+3);
               a8.remove(PurchasedItems.getSelectedRow()*4+2);
               a8.remove(PurchasedItems.getSelectedRow()*4+1);
               a8.remove(PurchasedItems.getSelectedRow()*4);   
               //********************************************
        }else if(Table9.isSelected())
        {
               a9.remove(PurchasedItems.getSelectedRow()*4+3);
               a9.remove(PurchasedItems.getSelectedRow()*4+2);
               a9.remove(PurchasedItems.getSelectedRow()*4+1);
               a9.remove(PurchasedItems.getSelectedRow()*4);   
               //********************************************
        }else if(Table10.isSelected())
        {
               a10.remove(PurchasedItems.getSelectedRow()*4+3);
               a10.remove(PurchasedItems.getSelectedRow()*4+2);
               a10.remove(PurchasedItems.getSelectedRow()*4+1);
               a10.remove(PurchasedItems.getSelectedRow()*4);   
               //********************************************
        }else if(Table11.isSelected())
        {
               a11.remove(PurchasedItems.getSelectedRow()*4+3);
               a11.remove(PurchasedItems.getSelectedRow()*4+2);
               a11.remove(PurchasedItems.getSelectedRow()*4+1);
               a11.remove(PurchasedItems.getSelectedRow()*4);   
               //********************************************
        }else if(Table12.isSelected())
        {
               a12.remove(PurchasedItems.getSelectedRow()*4+3);
               a12.remove(PurchasedItems.getSelectedRow()*4+2);
               a12.remove(PurchasedItems.getSelectedRow()*4+1);
               a12.remove(PurchasedItems.getSelectedRow()*4);   
               //********************************************
        }else if(Table13.isSelected())
        {
               a13.remove(PurchasedItems.getSelectedRow()*4+3);
               a13.remove(PurchasedItems.getSelectedRow()*4+2);
               a13.remove(PurchasedItems.getSelectedRow()*4+1);
               a13.remove(PurchasedItems.getSelectedRow()*4);   
               //********************************************
        }else if(Table14.isSelected())
        {
               a14.remove(PurchasedItems.getSelectedRow()*4+3);
               a14.remove(PurchasedItems.getSelectedRow()*4+2);
               a14.remove(PurchasedItems.getSelectedRow()*4+1);
               a14.remove(PurchasedItems.getSelectedRow()*4);   
               //********************************************
        }else if(Table15.isSelected())
        {
               a15.remove(PurchasedItems.getSelectedRow()*4+3);
               a15.remove(PurchasedItems.getSelectedRow()*4+2);
               a15.remove(PurchasedItems.getSelectedRow()*4+1);
               a15.remove(PurchasedItems.getSelectedRow()*4);   
               //********************************************
        }else if(Table16.isSelected())
        {
               a16.remove(PurchasedItems.getSelectedRow()*4+3);
               a16.remove(PurchasedItems.getSelectedRow()*4+2);
               a16.remove(PurchasedItems.getSelectedRow()*4+1);
               a16.remove(PurchasedItems.getSelectedRow()*4);   
               //********************************************
        }else if(Table17.isSelected())
        {
               a17.remove(PurchasedItems.getSelectedRow()*4+3);
               a17.remove(PurchasedItems.getSelectedRow()*4+2);
               a17.remove(PurchasedItems.getSelectedRow()*4+1);
               a17.remove(PurchasedItems.getSelectedRow()*4);   
               //********************************************
        }else if(Table18.isSelected())
        {
               a18.remove(PurchasedItems.getSelectedRow()*4+3);
               a18.remove(PurchasedItems.getSelectedRow()*4+2);
               a18.remove(PurchasedItems.getSelectedRow()*4+1);
               a18.remove(PurchasedItems.getSelectedRow()*4);   
               //********************************************
        }else if(Table19.isSelected())
        {
               a19.remove(PurchasedItems.getSelectedRow()*4+3);
               a19.remove(PurchasedItems.getSelectedRow()*4+2);
               a19.remove(PurchasedItems.getSelectedRow()*4+1);
               a19.remove(PurchasedItems.getSelectedRow()*4);   
               //********************************************
        }else if(Table20.isSelected())
        {
               a20.remove(PurchasedItems.getSelectedRow()*4+3);
               a20.remove(PurchasedItems.getSelectedRow()*4+2);
               a20.remove(PurchasedItems.getSelectedRow()*4+1);
               a20.remove(PurchasedItems.getSelectedRow()*4);   
               //********************************************
        }
     
     
 }
}
